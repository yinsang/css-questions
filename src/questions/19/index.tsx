import React from 'react';
import './index.module.scss';

interface Props {

}
const Box = ({}: Props) => {
  // window initial data

  // states

  // location hooks

  // functions

  // useEffect hooks

  // classVariables
  // render
  return (
    <div styleName='try'>
      给try-child设置color: red;
      <div styleName='try-child'>try-child</div>
    </div>
  );
};

export default Box;
