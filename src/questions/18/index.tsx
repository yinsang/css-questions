import React from 'react';
import './index.module.scss';

interface Props {

}
const Box = ({}: Props) => {
  // window initial data

  // states

  // location hooks

  // functions

  // useEffect hooks

  // classVariables
  // render
  return (
    <div styleName='try'>
      设置 title、desc、info之间垂直距离都为10px。忽略行高问题，直接写距离。
      <div styleName='title'>title</div>
      <div styleName='desc'>desc</div>
      <div styleName='info'>
        info
      </div>
    </div>
  );
};

export default Box;
