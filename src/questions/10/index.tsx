import React from 'react';
import './index.module.scss';

interface Props {

}
const Box = ({}: Props) => {
  // window initial data

  // states

  // location hooks

  // functions

  // useEffect hooks

  // classVariables
  // render
  return (
    <div styleName='try'>
      father 父元素里面的字体
      <div styleName='child'>
        child 儿子字体
      </div>
    </div>
  );
};

export default Box;
