import React from 'react';
import './index.module.scss';

interface Props {

}
const Box = ({}: Props) => {
  // window initial data

  // states

  // location hooks

  // functions

  // useEffect hooks

  // classVariables
  // render
  return (
    <div styleName='try'>
      <span>确定</span>
      <span>取消</span>
    </div>
  );
};

export default Box;
